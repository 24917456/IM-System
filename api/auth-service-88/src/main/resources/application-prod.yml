server:
  port: 88
  tomcat:
    uri-encoding: utf-8
    max-swallow-size: -1 #不限制请求体大小
spring:
  application:
    name: auth-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos

logging:
  level:
    root: info

